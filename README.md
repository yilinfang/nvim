# nvim

My neovim settings based on [LazyVim](https://github.com/LazyVim/LazyVim).

## Optional

To keep the secrets (github-copilot token, etc) safe, use following command to secure the secrets to some extent.

```bash
chmod 700 ~/.config/nvim/
```
